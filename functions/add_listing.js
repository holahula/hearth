const lib = require('lib');
const db = require('db')
/**
* A basic todo app
* @param {number} id
* @param {object} location
* @param {string} description
* @returns {string}
*/
module.exports = async (id, location, description, context) => {
  //
};